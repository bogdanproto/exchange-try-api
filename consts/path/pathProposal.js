const pathProposal = {
  ROOT: '/api/proposals',
  BASE: '/',
  RESERVATION: '/reservation',
  ID: '/:id',
  OWNER_UPD: '/update/:id',
};

Object.freeze(pathProposal);

module.exports = pathProposal;
