const pathUsers = {
  ROOT: '/users',
  USERS: '/',
  REGISTER: '/register',
  LOGIN: '/login',
  LOGOUT: '/logout',
  CURRENT: '/current',
  AVATAR: '/avatars',
};

Object.freeze(pathUsers);

module.exports = pathUsers;
